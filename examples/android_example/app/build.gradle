apply plugin: 'com.android.application'

android {
    compileSdkVersion 18
    buildToolsVersion '28.0.3'

    defaultConfig {
        applicationId "org.imgui.android"
        minSdkVersion 10
        targetSdkVersion 18

        ndk {
            moduleName 'main'
        }

    }

    sourceSets.main {
        jni.srcDirs = []
        jniLibs.srcDir 'src/main/libs'
    }
}
